val result = when (currentUnit) {
    // Conversion cases for current unit
}.let { convertedValue ->
    when (targetUnit) {
        // Conversion cases for target unit using the converted value
        else -> {
            println("Invalid target unit of measurement.")
            return
        }
    }
}
